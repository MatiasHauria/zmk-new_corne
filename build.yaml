include:
  # 🧠 LADO MASTER (generalmente left)
  - board: eyelash_corne_left
    shield: nice_view_custom
    snippet: studio-rpc-usb-uart
    cmake-args: -DCONFIG_ZMK_STUDIO=y -DCONFIG_ZMK_STUDIO_LOCKING=n
    artifact-name: eyelash_corne_left_with_bongo

  # 🔇 LADO SLAVE (sin bongo)
  - board: eyelash_corne_right
    # ❗ IMPORTANTE: sin nice_view_custom
    artifact-name: eyelash_corne_right_clean

  # 🔧 RESET
  - board: nice_nano_v2
    shield: settings_reset
